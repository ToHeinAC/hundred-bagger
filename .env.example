# Copy to .env and fill in. Scalars only — screening thresholds live in src/config.py.

# Required by SEC EDGAR from Phase 2 onward. SEC blocks requests without a real
# contact email in the User-Agent header.
SEC_USER_AGENT="Your Name your.email@example.com"

# Optional overrides.
# DUCKDB_PATH=data/100baggers.duckdb
# STREAMLIT_PORT=8501
